;; This is lish core

(def require (fn [x] (eval (str "(do " (cat x) ")"))))

;; increment
(def inc (fn [x] (+ x 1)))

;; map
(def map (fn [f lst]
          (if (empty? lst)
            []
            (cons (f (first lst))
                  (map f (rest lst))))))